{
  "name": "rabbitclone-worker",
  "version": "0.1.0",
  "private": true,
  "description": "Chromium X display worker that publishes the desktop to RTSP.",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "check": "node --check src/server.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^4.19.2",
    "pino": "^9.0.0",
    "playwright-core": "^1.44.0"
  }
}

